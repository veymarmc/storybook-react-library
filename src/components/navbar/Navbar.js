import React from 'react';
import PropTypes from 'prop-types';
import './navbar.scss';

export const Navbar = (props) => {
  return (
    <div>
      navbar
    </div>
  );
}

Navbar.propTypes = {
  
};

Navbar.defaultProps = {

};
